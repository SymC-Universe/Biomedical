{
  "schema_version": "0.2",
  "status": "PROSPECTIVELY_FROZEN_NUMERICAL_REPAIR_BEFORE_V02_OUTPUT",
  "authority": "SymC GOM v0.8.4",
  "preserved_predecessor": "BIO_CHI/config/JARUS_LOCAL_STABILITY_V01_RESULT_PIN.json",
  "scientific_question_changed": false,
  "parameter_cases_changed": false,
  "seed_set_changed": false,
  "root_acceptance_changed": false,
  "jacobian_rule_changed": false,
  "repair": {
    "before": "fsolve in log-state coordinates z with x=exp(z)",
    "after": "fsolve directly in physical state coordinates x; intermediate iterates may leave the positive orthant, but only final all-positive roots can be accepted",
    "mechanical_reason": "the v0.1 log transform produced nominal solver convergence with physical residuals around 1e-4 because dF/dz scales each column by x; the unchanged residual acceptance gate rejected every candidate"
  },
  "root_search": {
    "solver": "MATLAB fsolve in physical coordinates",
    "matlab_release": "R2023b",
    "product": "Optimization Toolbox",
    "random_generator": "twister",
    "random_seed": 20260923,
    "random_multistarts": 64,
    "random_seed_domain_per_state": "log-uniform on [1e-6, 10]",
    "additional_fixed_seeds": [0.0001, 0.001, 0.01, 0.1, 0.5, 1.0],
    "fsolve_function_tolerance": 1e-12,
    "fsolve_step_tolerance": 1e-12,
    "maximum_iterations": 2000,
    "accepted_root_residual_inf_norm": 1e-9,
    "accepted_root_final_state": "all finite and strictly positive",
    "root_cluster_relative_inf_tolerance": 1e-6,
    "no_posthoc_multistart_extension": true,
    "no_root_selection": true
  },
  "jacobian": {
    "method": "unchanged central finite differences at every accepted positive root",
    "base_step_per_coordinate": "machine_epsilon^(1/3) * max(1, abs(x_j))",
    "step_multipliers": [1.0, 0.5, 0.25],
    "classification": "STABLE only if max real eigenvalue <0 for all three steps; UNSTABLE only if >0 for all three; otherwise INDETERMINATE_NUMERICAL_SIGN"
  },
  "paper_claim_adjudication": {
    "FIG8A_NOMINAL_DAMPED": "PASS only for exactly one accepted positive root classified STABLE",
    "FIG8B_LIMIT_CYCLE": "PASS only for exactly one accepted positive root classified UNSTABLE",
    "FIG8C_RELAXATION_OSCILLATION": "PASS only for exactly one accepted positive root classified UNSTABLE"
  },
  "guards": {
    "v0_1_indeterminate_preserved": true,
    "finite_window_results_preserved": true,
    "no_trajectory_state_used_as_seed": true,
    "no_threshold_retuning": true,
    "no_mode_selection": true,
    "no_chi_bio_construction": true
  }
}
