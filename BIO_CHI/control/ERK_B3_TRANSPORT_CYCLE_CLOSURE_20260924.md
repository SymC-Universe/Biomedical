# Bio Chi ERK B3 transport-cycle closure

**Date:** 24 September 2026  
**Branch:** `bio-chi-closure-p0q-20260924`  
**Status:** CURRENT ERK CROSS-SYSTEM P0-Q CYCLE CLOSED AT PARTIAL TRANSPORT  
**Authority:** SymC General Operations Manual v0.8.6

## Closed result

The independent Blum 2019 B3 deterministic ERK system supports transport of the **mode-resolved scalar construction** but does not support admission of the complete ERK modal `Chi_bio` under the frozen local nonlinear round-trip gate.

The cycle therefore closes as:

```text
chi_bio representation class transport: SUPPORTED at P0-Q
ERK model-specific Chi_bio admission: REFUSED under current frozen gate
broad biological Chi_bio transport: NOT ESTABLISHED
Bio Chi cross-system transport: NOT OPENED
P1 confirmation: NOT CLAIMED
```

## Evidence spine

1. Publication-selected B3 deterministic model source and parameter roles were frozen before SymC generator analysis.
2. The source-native B3 trajectory was independently reproduced within the frozen numeric thresholds.
3. Conservation-law directions were removed from modal interpretation by the prospectively frozen stoichiometric dynamic-subspace construction.
4. The v0.1 finite-difference derivative route failed its frozen numerical rule and remains preserved.
5. The v0.2 analytic-Jacobian repair was independently cross-checked by complex-step differentiation and passed.
6. The 8D reduced generator is fully FRET-observable in all frozen source-defined lanes/contexts.
7. Every FRET-visible complex invariant factor supports the same program-wide scalar construction:
   `chi_bio_i = -Re(lambda_i)/|lambda_i|`.
8. The source-defined parameter lanes and inputs produce different complex-pair counts and values. Transport is therefore of the representation class, not one preferred numeric chi.
9. Every transported complex-pair lane passed the frozen local nonlinear round-trip.
10. The complete 8D full-modal round-trip failed the frozen all-direction rule in every context.

## Scalar transport that survives

The generator-derived scalar class transports from NF-kB to ERK without requiring the same numeric value, the same number of complex pairs, or the same biological pathway.

Frozen ERK examples include:
- primary, FGF 2.5: `chi_bio ~= 0.78956`;
- primary, FGF 250: `chi_bio ~= 0.99874` and `0.44686`;
- sensitivity, FGF 2.5: `chi_bio ~= 0.98762` and `0.72565`;
- sensitivity, FGF 250: `chi_bio ~= 0.98384` and `0.73996`.

These are mode-specific factors only. No system scalar and no chi=1 biological boundary are inferred.

## Modal refusal that must remain visible

The frozen complete 8D nonlinear round-trip failed because some predeclared source-stoichiometric directions did not show the required 1e-3 to 1e-4 error decrease.

Failing columns:
- primary FGF 2.5: 4, 6, 10, 14;
- primary FGF 250: 10, 12, 14;
- sensitivity FGF 2.5: 6, 10, 14;
- sensitivity FGF 250: 10, 12, 14.

Columns 10 and 14 fail across all four contexts. Columns 4, 6, and 12 are context dependent.

No unfavorable direction is dropped, no amplitude is changed, and no alternative subspace is selected after viewing this result.

## Scientific interpretation

The result supports a narrower and more useful cross-system statement than a blanket modal transport claim:

> A generator-derived, observable complex-mode scalar construction can transport across distinct biological signaling systems even when the complete higher-dimensional modal architecture does not satisfy the same qualification rule.

This preserves the GOM hierarchy rather than forcing all representation layers to succeed together.

The NF-kB system remains the currently admitted model-specific three-object example:
- `chi_bio`: admitted P0-Q local complex-factor scalar;
- `Chi_bio`: admitted P0-Q complete local six-mode representation;
- Bio Chi: admitted P0-Q relational system architecture.

The ERK B3 system currently contributes:
- independent transport of the `chi_bio` representation class;
- an observable, generator-qualified modal precursor;
- a negative Limit Map result for complete 8D `Chi_bio` nonlinear qualification.

## Re-entry condition

Any attempt to admit ERK `Chi_bio` after this refusal requires a **new P0-Q lineage** with an independently justified scientific basis, such as a different native modal object, carrier decomposition, state representation, or qualification question. It may not:
- delete failing directions;
- retune perturbation amplitudes because of the observed failure;
- choose only favorable complex modes;
- reinterpret the failed gate as passed;
- use the same outcome to select a replacement representation and count it as independent support.

A new lineage may use this failure as qualification evidence, but it inherits explicit promotion debt.

## Current scientific stop

No further broad Bio Chi transport test is opened from this lineage because its required ERK modal layer did not pass.

Further progress now requires a scientific decision: either
1. open a new P0-Q ERK modal-representation lineage motivated by a domain-native reason independent of favorable outcome selection; or
2. preserve the refusal and move to a different independent biological system for a fresh modal/Bio Chi transport attempt.

Until that choice is made, the current ERK cycle is closed.
