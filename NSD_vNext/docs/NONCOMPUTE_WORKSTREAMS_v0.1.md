# NSD Non-Computational Workstreams v0.1

Status: ACTIVE / MAJOR PRE-COMPUTE DESIGN PASS COMPLETE
Date: 14 September 2026

These are the workstreams that can proceed without waiting for currently unavailable or unfinished NSD computation.

## Current closure matrix

| # | Workstream | Current state | Forward dependency |
| --- | --- | --- | --- |
| 1 | historical evidence reconstruction | `SOURCE-LEVEL COMPLETE FOR AVAILABLE MATERIAL` | original plot/data code only if it ever reappears; do not block forward work |
| 2 | estimator and notation audit | `DESIGN COMPLETE / LITERATURE-CONSTRAINED` | computation needed for target-data model qualification |
| 3 | dataset provenance and hierarchy audit | `AUDIT SCHEMA COMPLETE / POPULATION BLOCKED` | exact empirical dataset/source manifests |
| 4 | healthy baseline design | `SPECIFICATION COMPLETE` | audited healthy datasets + analysis |
| 5 | Structural Engine specification | `SPECIFICATION COMPLETE` | implementation + qualification computation |
| 6 | known-truth/adversarial qualification design | `DESIGN COMPLETE` | computation |
| 7 | Function Map design | `DESIGN COMPLETE / EMPIRICAL OCCUPANCY PENDING` | data + Engine results |
| 8 | Limit Map design | `DESIGN COMPLETE / EMPIRICAL BOUNDARIES PENDING` | known-truth + real-data results |
| 9 | native comparator program | `DESIGN COMPLETE` | task-specific frozen analyses |
| 10 | cross-disorder design | `DESIGN COMPLETE` | multiple audited disorder datasets |
| 11 | comorbidity design | `DESIGN COMPLETE` | suitable labels/symptom/comorbid data |
| 12 | manuscript development | `ACTIVE / METHODS AND HISTORICAL RECONCILIATION ADVANCED` | result sections remain gated |
| 13 | figure/table architecture | `DESIGN PASS COMPLETE` | empirical figures blocked by evidence |
| 14 | reproducibility planning | `STRUCTURE COMPLETE / IMPLEMENTATION CONTINUES WITH CODE` | production pipeline |
| 15 | experimental-opportunity program | `INITIAL DESIGN + COLLISION COMPLETE` | targeted residual prior-art searches and, later, experiments/data |
| 16 | publication/novelty audit | `MAJOR INITIAL PASS COMPLETE` | targeted residual-novelty search before final novelty wording |

## 1. Historical evidence reconstruction

Completed from the material currently available:
- exact main and supplementary TeX recovered;
- rendered PDFs cross-checked;
- file hashes recorded;
- figures classified by evidentiary status;
- historical exact values and thresholds prevented from automatic transfer;
- lack of claim-level inline citation binding documented.

Canonical output: `HISTORICAL_SOURCE_AUDIT_v0.1.md`.

Remaining nonblocking gap:
- original code/data package used for any historical plotted input, if one ever existed in recoverable form.

Forward rule:
Do not spend the rebuild waiting for that package. Regenerate every empirical result from current traceable sources.

## 2. Estimator and notation audit

Completed design/output:
- native observable versus model parameter separated;
- center frequency versus `omega_0` firewall;
- Gaussian width versus dynamical damping firewall;
- linewidth/Q conditional interpretation;
- Q/chi dependency control;
- aperiodic exponent retained as empirical spectral coordinate, not direct gamma or chi;
- dynamically derived / operational / proxy / empirical / categorical / refusal states defined;
- direct oscillator and pole-based routes retained as model-conditional candidates.

Canonical output: `ESTIMATOR_LICENSING_MATRIX_v0.1.md`.

Next dependency:
Known-truth and target-data qualification.

## 3. Dataset provenance and hierarchy audit

Schema completed:
- source/version/license;
- subject/session/run/trial/channel hierarchy;
- acquisition condition;
- site/device;
- age/sex/medication/state where available;
- exact metadata-join cardinality;
- exclusions/missingness;
- split manifest;
- pseudo-replication and leakage checks.

Canonical output: `DATASET_PROVENANCE_AUDIT_v0.1.md`.

Blocked item:
Population of the schema requires the exact forward empirical datasets or their existing manifests/code.

Historical continuity markers such as the approximately 1,437 sessions remain unverified until that source lineage is recovered.

## 4. Healthy baseline design

Completed specification:
- one versioned reusable healthy/reference construction;
- Engine-version binding;
- age/site/acquisition/state covariates;
- spatial and modal distributions;
- scalar distributions only where licensed;
- refusal/absence prevalence;
- trait/state decomposition;
- Function/Limit Map integration;
- disease overlays prohibited from retuning the baseline.

Canonical output: `NEUROSTABILITY_ATLAS_SPEC_v0.1.md`.

Next dependency:
Audited healthy data and Engine outputs.

## 5. Structural Engine specification

Completed production-facing design:
- E0 provenance gate;
- E1 signal integrity;
- E2 spectral state;
- E3 candidate modal extraction;
- E4 scalar admission;
- E5 spatial/conglomerate construction;
- E6 ordinary-state/reliability mapping;
- E7 clinical overlay only after structural qualification;
- machine-readable refusal codes;
- no-label construction firewall;
- versioning rules.

Canonical output: `STRUCTURAL_ENGINE_SPEC_v0.1.md`.

Next dependency:
Implementation and computation.

## 6. Known-truth and adversarial qualification design

Completed design includes:
- true single oscillator;
- separated and unresolved multiple modes;
- no-peak/aperiodic-only signals;
- descriptive Gaussian peak without dynamical generator;
- frequency drift;
- burst oscillator;
- nonlinear/limit-cycle dynamics;
- colored input;
- coupled spatial modes;
- window/resolution/line-noise adversaries;
- peak-shape mismatch;
- volume conduction/reference/channel dropout;
- site/device shift;
- session duplication;
- label leakage;
- class imbalance;
- healthy-baseline retuning;
- conflicting estimators.

Canonical output: `KNOWN_TRUTH_ADVERSARIAL_TEST_MATRIX_v0.1.md`.

Next dependency:
Execution.

## 7. Function Map design

Design targets already defined:
- healthy occupancy;
- session/repeated-state stability;
- periodic/aperiodic organization;
- spatial organization;
- mode count and absent modes;
- licensed modal/scalar relations;
- age/development;
- perturbation/recovery;
- comorbidity and cross-disorder structure where data permit.

Canonical output: `FUNCTION_LIMIT_MAP_v0.1.md` plus Atlas specification.

Next dependency:
Empirical occupancy.

## 8. Limit Map design

Design includes:
- poor spectral fit;
- no valid peak;
- unresolved modes;
- insufficient frequency resolution;
- non-damping broadening;
- failed scalar reduction;
- ambiguous metadata;
- site/acquisition transfer failure;
- insufficient spatial coverage;
- out-of-domain state;
- native baseline matching or beating NSD;
- failure of shared-architecture transfer.

Canonical output: `FUNCTION_LIMIT_MAP_v0.1.md`, Engine refusal codes, and adversarial matrix.

Next dependency:
Measured failure boundaries.

## 9. Native comparator program

Completed design:
- C0 demographic/acquisition;
- C1 simple spectral;
- C2 periodic/aperiodic;
- C3 spatial;
- C4 native modal/state-space;
- C5 clinical;
- nested NSD additions;
- task-specific ADDS/EQUIVALENT/SUBTRACTS/INDETERMINATE/NOT_TESTED decisions.

Canonical output: `NATIVE_COMPARATOR_PROGRAM_v0.1.md`.

Next dependency:
Freeze exact comparator for each empirical task before decisive evaluation.

## 10. Cross-disorder design before adding disorders

Completed design distinguishes:
- shared marginal shifts;
- shared multivariate geometry;
- preserved disorder-specific internal organization;
- shared transition/recovery structure;
- partial overlap;
- no common architecture;
- non-comparability.

Canonical output: `CROSS_DISORDER_COMORBIDITY_TEST_PLAN_v0.1.md`.

Next dependency:
At least two materially distinct audited disorder datasets using the same frozen Engine/Atlas.

## 11. Comorbidity analysis design

Completed separation:
- epidemiological diagnostic overlap;
- symptom-dimensional overlap;
- physiological overlap;
- architecture interaction;
- longitudinal transition.

Competing explanations and null models remain live.

Canonical output: `CROSS_DISORDER_COMORBIDITY_TEST_PLAN_v0.1.md`.

Next dependency:
Suitable comorbid, symptom, and physiological source data.

## 12. Manuscript development

Advanced now:
- abstract reframed;
- introduction includes historical evidentiary correction;
- architecture rewritten around native signal/modal/system structure;
- aperiodic and bandwidth firewalls incorporated;
- prior-art dynamical route acknowledged;
- historical source reconciliation moved from pending to completed at source level;
- strong 2025 claims retired from forward Results;
- clinical ceiling and limitation language strengthened.

Canonical output: `manuscript/NSD_MANUSCRIPT_WORKING_v0.1.md`.

Still gated:
- healthy results;
- ASD/current first phenotype results;
- cross-disorder results;
- comorbidity results;
- prediction;
- empirical Function/Limit Maps.

## 13. Figure/table architecture

Completed design:
- historical figure firewall;
- architecture figure;
- scalar-admission firewall;
- hierarchy/provenance figure;
- Engine + Atlas figure;
- known-truth/refusal figure;
- Function/Limit figure;
- historical-to-forward reconstruction figure;
- table structure for estimator licensing, historical claims, qualification, Atlas, novelty, comparators, reproducibility.

Canonical output: `FIGURE_TABLE_PLAN_v0.1.md`.

Empirical disease plots remain blocked by evidence, intentionally.

## 14. Reproducibility planning

Existing structure:
- R1 release integrity;
- R2 scientific recomputation;
- R3 source/provenance reconstruction;
- manifests/hashes/versioning;
- planned data identity and split registries;
- refusal/result provenance.

Canonical output: `REPRODUCIBILITY_MATRIX_v0.1.md`.

Next dependency:
Implementation must emit these artifacts automatically rather than relying on manuscript prose.

## 15. Experimental-opportunity program

Initial experiment design and collision are now integrated.

Highest-value remaining experiment families:
1. explicit dynamical-model qualification versus broadening adversaries;
2. refusal-status repeatability;
3. repeated-session multilevel stability architecture;
4. spatial/modal preservation versus global summaries;
5. multimodal validation;
6. shared-versus-specific transdiagnostic architecture;
7. incremental longitudinal prediction only after earlier layers qualify.

Canonical outputs:
- `EXPERIMENTAL_OPPORTUNITY_PLAN_v0.1.md`
- `LITERATURE_COLLISION_PASS_v0.1.md`
- `LITERATURE_COLLISION_ADDENDUM_v0.1.md`

## 16. Publication/novelty audit

Major initial collision completed against:
- RDoC;
- HiTOP;
- p-factor / general psychopathology;
- normative modeling;
- EEG normative deviations and psychiatric heterogeneity;
- periodic/aperiodic EEG;
- transdiagnostic EEG;
- oscillator/Q/pole neural models;
- generic EEG classification/prediction.

Current result:
Many broad 2025 claims are established prior-art families and cannot carry the paper's novelty.

Candidate residual contribution is narrower:
- dynamically licensed/refused local scalars;
- integrated spectral/modal/spatial/conglomerate/open-channel architecture;
- independent label-blind Engine + Atlas governance;
- scalar-admission/refusal as retained information;
- known-truth qualification before clinical geometry;
- shared-instability test requiring preserved disorder-specific organization.

Canonical output: `NOVELTY_ATTRIBUTION_AUDIT_v0.1.md`.

Status: candidate residual novelty only. Targeted searches remain necessary before using “novel,” “first,” or equivalent language.

## Updated operating order

The immediately executable non-compute sequence has now been substantially exhausted:

`historical source reconstruction [done for available sources]`
-> `estimator licensing [done as design]`
-> `dataset audit schema [done]`
-> `Atlas design [done]`
-> `Engine specification [done]`
-> `known-truth/adversarial design [done]`
-> `Function/Limit design [done]`
-> `native comparators [done]`
-> `cross-disorder/comorbidity design [done]`
-> `manuscript/figure/reproducibility architecture [advanced]`
-> `initial experimental/literature/novelty collision [done]`.

## What remains possible without computation

Non-compute work is not literally exhausted, but the remaining useful work is narrower:
- deeper targeted prior-art search for refusal/non-identifiability as phenotype;
- targeted prior-art search for integrated multimodal/modal stability architectures;
- exact dataset/source recovery if legacy empirical files are found;
- prose/citation polishing of method sections;
- preregistration-style task specifications once the first forward dataset is fixed;
- implementation planning down to file schemas and tests.

The next large increase in scientific evidence requires **data/code execution**, not more confident prose.

No result section will be pre-written positive while those computations remain undone.