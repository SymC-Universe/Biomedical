# GRI v0.7.1A function / limit integration

**Date:** 2026-09-10  
**Status:** PROSPECTIVE PROGRAM-CONTROL ADDENDUM  
**Authority:** `General_Cross_Project_Research_Protocol_v0.7.1_FINAL_plus_v0.7.1A_Addendum.pdf`  
**Applies prospectively:** yes  
**Retroactive change to existing GRI results:** no

## 0. Purpose

This record operationalizes the v0.7.1A Functional Mapping and Natural Limit-Testbed Addendum for the current GRI program without changing any frozen or already-observed GRI result.

The addendum requires balanced learning about both:

- how a supported system/representation behaves where it works; and
- where, how, and why that representation degrades, transforms, refuses, or fails.

For GRI this creates a paired research target:

`GRI FUNCTION MAP + GRI LIMIT MAP`

Neither map is allowed to erase the other.

---

# 1. Research-mode classification

## 1.1 P0-D: discovery and mechanism mapping

Use P0-D for open-ended GRI work intended to understand the supported interior or qualified natural limit contexts, including:

- response surfaces across cancer types / cohorts / modalities;
- how scalar, modal/vector, conglomerate/system, and cross-component objects vary;
- coupling and redistribution across methylation, RNA, protein, genomic, and composition layers;
- alternative representations or transformations;
- candidate substrate relationships;
- candidate additional architectural objects;
- post-hoc diagnostics and visualizations;
- already-viewed data used to generate hypotheses;
- source-qualified rare natural contexts used as high-information exploratory limit probes.

P0-D may learn from existing TCGA evidence but may not convert those discoveries into confirmatory evidence for themselves.

## 1.2 P0-Q: qualification and controlled iteration

Use P0-Q for testing whether a candidate GRI System Model / Engine component is fit to face confirmation, including:

- known-truth synthetic systems;
- adversarial synthetic systems;
- previously observed failures;
- sensitivity analysis;
- identifiability analysis;
- negative controls;
- mutation tests;
- repeated-state equivalence tests;
- capacity/comparator qualification on already-viewed evidence;
- refusal-boundary qualification.

The currently frozen post-C1 adversarial sensitivity v2.2 is classified as **P0-Q**, not P1. It is a qualification/adversarial analysis of an already observed internal result and cannot promote that same result to prospective confirmation.

The future production Engine P2 qualification sequence inherits P0-Q evidence for engineering fitness but still requires separate P1 evidence for confirmatory scientific claims.

---

# 2. GRI Function Map

**Status:** current internal map is exploratory / internally supported, not externally confirmed.

The Function Map asks what happens where the current representation is supported.

## 2.1 Existing function-map evidence

Current internal evidence can already populate a development Function Map with the following non-causal objects:

- methylation spectral organization above construction-aware nulls;
- mode-resolved methylation structure and feature participation;
- patient-level methylation/RNA global geometry;
- Hallmark-level same-patient coupling where supported;
- context-sensitive changes after purity/leukocyte projection;
- internal predictive information from methylation Hallmark features beyond the two measured covariates used in the frozen comparator;
- an internally held-out relationship between frozen global geometry confidence and prediction risk;
- cancer-specific variation in representation performance.

These are function-map observations, not automatically mechanisms.

## 2.2 Function-map questions now authorized for P0-D

Without changing current frozen science, future P0-D work may map:

1. which cancer types occupy similar or distinct global methylation/RNA geometry regions;
2. how modal concentration / effective rank changes across cancers and contexts;
3. whether several different internal modal organizations produce similar predictive risk;
4. where context adjustment changes geometry strongly versus minimally;
5. whether Hallmark structure adds interpretable organization beyond equally sized unstructured or alternative feature representations;
6. how model performance varies continuously across geometry, sample support, composition completeness, missingness burden, and platform history;
7. whether representation redundancy or redistribution explains stable predictions despite internal modal changes;
8. which observed states are common versus sparsely occupied within the available development evidence.

Any favorable region discovered after inspection remains P0-D until frozen and tested on untouched evidence.

---

# 3. GRI Limit Map

**Status:** active qualification target.

The Limit Map asks where the current GRI representation changes, degrades, becomes non-identifiable, exits the model class, or should refuse.

## 3.1 Existing limit-map evidence

Current retained boundaries include:

- historical `CV/2` biological-chi interpretation: retired;
- rank ordering used as time: retired;
- nonlinear-only shared dependence: unsupported in current linear v0.1 scope;
- regulatory-autonomy scalar: redundant / dropped;
- global CKA/shared-private decomposition: established primitive, not a novelty basis;
- strong general Hallmark-semantic sharing: failed promotion;
- insufficient promoter-core mapping: structural refusal;
- insufficient composition-complete sample support: `NOT_EVALUABLE` rather than rescue;
- HM27/HM450 platform history: unresolved external-generalization risk;
- biological chi: not admitted;
- temporal inheritance/recovery: not tested from static TCGA;
- PCPG global transport: recurrent stress case in an independently source-qualified rare tumor type, preserved without post-hoc exclusion;
- raw cross-cancer Delta_CKA as a universal scalar bridge: retired after null-floor analysis;
- current two-covariate predictive comparator: informative but not capacity-matched.

## 3.2 Current primary P0-Q limit test

The post-C1 adversarial sensitivity v2.2 package is the immediate Limit-Map qualification test.

Its first obligation is state equivalence. No downstream adversarial result is interpretable unless the 6,400 canonical cancer/track/resample states reproduce to the frozen tolerance.

If state equivalence passes, the package may test the already-frozen missingness, mode-ablation, CKA-floor, repeated-null, TSS200, composition-complete, and conditioning attacks.

Outcomes must be retained whether favorable, unfavorable, mixed, indeterminate, or mechanically invalid.

---

# 4. Coverage architecture

The addendum defines four research roles. For GRI they are instantiated prospectively as follows.

| Research role | Current GRI status | Notes |
|---|---|---|
| `NOMINAL_FUNCTION` | **AVAILABLE INTERNALLY / NEEDS EXTERNAL CONFIRMATION** | Cancer/cohort regimes where the frozen architecture is evaluable and behaves within supported assumptions. |
| `PERTURBED_FUNCTION` | **PARTIAL** | Existing context-adjustment, technical-mask, missingness, and synthetic/adversarial tracks probe perturbed but still interpretable conditions. External paired primary/metastasis and treatment-resistance source families have now also been identified for future P0-D mapping. |
| `BOUNDARY_OR_TRANSITION` | **ACTIVE CANDIDATE MAP** | PCPG, insufficient mapping/sample support, nonlinear-only scope, platform dependence, and reduction-adequacy failures contribute boundary candidates. These are not all physical transitions. |
| `RARE_NATURAL_LIMIT` | **PCPG ADMITTED FOR POST-RESULT P0-D LIMIT MAPPING ONLY** | Rarity is independently established by oncology-native evidence, but the choice to investigate PCPG as a GRI limit was post-result and therefore carries promotion debt and no confirmatory weight. |

These are research roles, not biological phases.

---

# 5. PCPG disposition under v0.7.1A

PCPG remains scientifically interesting because it repeatedly stresses the global transport/prediction representation.

The first migration pass correctly refused to infer rarity from Engine behavior. A subsequent source-of-record audit then established rarity **independently of GRI**:

- NCI reports pheochromocytoma/paraganglioma incidence on the order of 2 to 8 diagnoses per million people per year;
- NCI's TCGA studied-cancer material independently calls the tumor type rare;
- the TCGA molecular characterization describes a 173-patient PCC/PGL cohort and substantial internal molecular heterogeneity.

Therefore:

`RECURRENT_ENGINE_STRESS_CASE + INDEPENDENT_DOMAIN_RARITY -> P0-D RARE_NATURAL_TESTBED`

but **not**:

`P0-D RARE_NATURAL_TESTBED -> CONFIRMATORY LIMIT RESULT`.

Current required state:

- `rarity_basis = DOMAIN_NATIVE_ONCOLOGY_EVIDENCE`;
- `selection_status = POST_RESULT_GRI_STRESS_CASE`;
- `rare_natural_testbed_status = ADMITTED_FOR_P0_D_LIMIT_MAPPING_ONLY`;
- `confirmation_status = NOT_CONFIRMATORY`;
- `promotion_debt = OPEN`.

PCPG may now be used as a high-information exploratory/qualification limit probe. It may help reveal whether global representation failure relates to subtype heterogeneity, regulatory organization, composition, driver class, or another measurable feature. Those explanations are hypotheses, not current results.

A future P1 rare-limit claim requires a new frozen prediction and an untouched rare case/cohort selected independently of the future Engine result.

Dedicated source/provenance record:

`artifacts/GRI_PCPG_RARE_NATURAL_TESTBED_P0D_20260910.md`

---

# 6. Pathway-specific independence map

Independence is recorded by the circularity pathway relevant to each claim rather than collapsed into one global label.

Minimum GRI dimensions:

- **data independence**;
- **cohort/system independence**;
- **outcome independence**;
- **parameter/tuning independence**;
- **method independence**;
- **Atlas independence**;
- **source/literature independence**;
- **temporal independence** where a temporal claim is attempted.

The controlling question is:

> Could the shared information on this pathway force the agreement being presented as evidence?

Examples:

- FINAL_HOLDOUT has meaningful internal outcome/partition isolation but remains within the same broad TCGA program/cohort lineage and is therefore not external cohort independence;
- current TCGA methylation/RNA evidence is non-independent for validating an Atlas or Engine built from the same evidence;
- use of the same frozen mathematical coordinate transformation does not by itself destroy data independence;
- PCPG rarity evidence is independent of GRI output, but PCPG's selection for GRI limit analysis is post-result;
- shared nuisance metadata can be acceptable when it cannot force the decisive agreement, but the shared path must remain visible.

Future MFR-10 and Atlas records store these independence dimensions separately.

---

# 7. Function/Limit output extension for the future GRI Engine

The P2 output schema adds two explicit top-level groups.

## 7.1 `function_map_output`

Contains, where supported:

- explicit `scope_id` and component/claim referent;
- supported operating regime;
- observed response surfaces / trajectories / occupancy distributions;
- supported cross-component relationships;
- uncertainty;
- identifiability;
- reduction information loss;
- evidence class;
- discovery versus confirmatory status.

## 7.2 `limit_map_output`

Contains, where supported:

- explicit `scope_id` and component/claim referent;
- degradation/transition/refusal region;
- first failing component or relationship, if identifiable;
- scalar/modal/conglomerate reduction-adequacy status;
- rare-natural-testbed status when independently qualified;
- whether the limit appears physical, inferential, estimator-related, data-availability-related, or unresolved;
- relevant independence class;
- uncertainty/tolerance;
- current epistemic status.

The mature Tool distinguishes at least:

- `WORKS_HERE`
- `STOPS_WORKING_HERE`
- `NOT_KNOWN_HERE`

Each state applies only to its explicit scope. A supported layer cannot silently promote an unsupported layer, and a limit in one layer cannot erase an unrelated narrower supported result.

---

# 8. Milestone balance check for current GRI state

## Question 1: Have we learned how the system behaves where the representation works?

**YES, internally, to a meaningful but incomplete degree.**

The program has mapped substantial static multiomic organization and internal held-out predictive behavior. It therefore should not become purely failure-seeking.

## Question 2: Have we meaningfully challenged where the representation stops working?

**YES, but the post-C1 sensitivity is still needed.**

The program has retained multiple failures and stress cases, and PCPG now supplies a legitimate rare-natural P0-D limit context. Current C1 robustness still needs the already-frozen adversarial sensitivity disposition.

## Balance decision

The immediate sequence remains:

1. finish the P0-Q post-C1 sensitivity because that qualification gate is already frozen and unresolved;
2. in parallel continue non-heavy Function-Map organization and source discovery on already observed/development evidence;
3. preserve PCPG as a rare but nonrepresentative post-result limit probe;
4. use newly identified paired primary/metastasis and treatment-resistance datasets as P0-D source candidates for genuine perturbed-function mapping without prematurely designating them as P1 tests;
5. after sensitivity closure, avoid an endless succession of boundary-only attacks unless a specific unresolved limit requires them;
6. preserve every newly discovered favorable region as P0-D until an independent freeze/confirmation path is defined.

This is not an equal positive/negative quota. It is scientific-coverage balance.

---

# 9. Effect on current frozen/historical GRI work

The v0.7.1A addendum applies prospectively.

Therefore it does **not**:

- alter F4 `NARROW`;
- alter legacy predictive P0 FINAL_HOLDOUT outcomes;
- alter Stage C1 C1-3 completion;
- rescue prior failed predictions;
- promote prior post-result discoveries;
- change the frozen post-C1 sensitivity design;
- admit biological chi;
- change manuscript claims automatically.

It changes how the next work is organized and what a mature GRI Tool must eventually expose.

---

# 10. Project-control state

**STATUS:** v0.7.1 + v0.7.1A is the current governing protocol package for future GRI decisions; prior GRI results retain their existing epistemic status; PCPG is source-qualified only as a post-result P0-D rare-natural limit testbed.  
**CURRENT GATE:** P0-Q post-C1 adversarial sensitivity v2.2 remains the unresolved heavy-compute qualification gate.  
**NEXT ACTION:** finish sensitivity to disposition; in parallel continue Function/Limit mapping, source discovery, pathway-specific independence, comparator selection-route work, and P2 protocol-neutral qualification.  
**WHY:** the addendum requires learning both the supported interior and the limits without weakening the frozen claim boundary.  
**ACTIVE EXECUTION:** protocol migration branch/PR, protocol contract tests, Function/Limit/Atlas architecture, comparator provenance/survey, external P0-D source inventory.  
**USER ACTION:** NONE for repository-side migration unless a new scientific freeze, comparator design, external cohort selection, or interpretation change is required.  
**SUCCESS CONDITION:** sensitivity disposition complete; Function/Limit architecture represented and scoped; historical results not retroactively relabeled; science-changing decisions explicitly frozen only after review.  
**FAILURE BRANCH:** mechanical failures are repaired under existing rules; scientific failures remain in the Limit Map; indeterminate results remain visible; post-result rare-limit findings do not become confirmation.  
**NEXT GATE:** post-sensitivity scope freeze for the candidate System Model/Engine, followed by production P2 qualification and independent Atlas/external-validation design.