# GRI Chi_bio admission-cycle closure

**Date:** 2026-09-13  
**Status:** CURRENT ADMISSION CYCLE CLOSED; `Chi_bio` REMAINS `NOT_ADMITTED`  
**Branch:** `gri-v071-protocol-integration-20260910-chi-bio`  
**Input head:** `c95d071999ba0d4c601f190181eeda8137aa4571`  
**Protocol closure authority:** General Cross-Project Research Protocol v0.7.7  
**Machine record:** `config/gri_Chi_bio_admission_cycle_closure_20260913_v1.json`

## 1. Decision

Close the current `Chi_bio` admission-or-falsification cycle at **State A: NOT_ADMITTED**.

This is the preferred scientific disposition because the prospectively frozen B3 construction did not survive its independent identifiability holdout, the obvious outcome-free graph-mode shortcut does not provide a compact natural basis, normalized empirical G1 remains independently blocked by unresolved restoration, and the G2 temporal program has already delivered the bounded information it can support without changing its scientific definition.

No new post-result rescue representation is opened in this cycle. Opening one now would create a new lineage and new multiplicity debt after inspecting multiple failures. That may be scientifically legitimate in a future P0-D/P0-Q program, but it is not a lawful way to convert this cycle into an admitted coordinate.

Final state:

```text
Chi_bio_status = NOT_ADMITTED
Chi_bio_value = null
Chi_bio_unity_boundary_status = NOT_TESTED
biological_stability_regime_label = WITHHELD
candidate_locked = false
```

The program specification explicitly allowed this outcome and required stopping before candidate freeze if no candidate survived derivation and identifiability strongly enough to justify C5.

## 2. Evidence that closes the cycle

### 2.1 Short-term G2 lane

The frozen short-term SCC25 lane closed by prospective refusal:

- workflow run `34756865980`;
- artifact `10317941541`;
- artifact SHA-256 `9b6890012183f6d173df4092af1629098d6bd9591490c57c53e92a270f0be55f`;
- D1 predictive adequacy refused at both `r=2` and `r=3`;
- D2 was not opened under the frozen escalation rule;
- no biological unity or stability-boundary inference was licensed.

### 2.2 Chronic G2 lane

The independent weekly SCC25 lane produced bounded architecture evidence:

- primary workflow run `34769901215`;
- primary artifact `10321342847`;
- primary artifact SHA-256 `4ad76198657cbb4f5a5839b5ad4068625ccb0801fe050d16b83006d2ef8a5687`;
- bounded feature-gate robustness run `34770059647`;
- robustness artifact `10321991562`;
- robustness SHA-256 `7a6a289d14ac7129cce979f340cb54fbbde7b3e767d411c243af76af1c7afed2`.

The findings that survive the bounded feature-universe challenge are narrow and useful:

1. D2 treatment-specific operator reorganization is `NOT_SUPPORTED` at `r=2` and `r=3` across feature gates `0.1`, `1.0`, and `10`.
2. The complete D1 spectral-radius sensitivity envelope remains mathematically `BELOW` the discrete-time unit circle at all three feature gates and both ranks.
3. `r=2` D1 predictive adequacy remains `PASS` across the bounded feature gates.
4. `r=3` predictive adequacy fails at the stringent `10` gate, and the non-normal/transient warning remains representation-sensitive.

Therefore the chronic lane supports a restricted weekly transition-architecture result, not an admitted biological Chi, not a biological critical boundary, and not rank-independent transfer of every conclusion.

### 2.3 B3 matched-network identifiability

The prospectively frozen signed-regulon ULM plus outcome-blind control-PCA construction failed the unseen-seed qualification:

- workflow run `34797932432`;
- artifact `10330711216`;
- artifact SHA-256 `02562ee9f1b47d497529b1e60a3c7df295cbf3b29d8983d1b50271cd189f4298`;
- status `REFUSE_B3_MATCHED_NETWORK_IDENTIFIABILITY`;
- passing dimensions `[]`;
- selected dimension `null`;
- promotion effect `NONE`.

Real SCC25/TCGA expression outcomes remained closed for representation selection. The refusal is scientific, not mechanical.

The failed construction is not repaired by loosening the frozen latent-angle rule, loosening spectral-radius recovery, dropping unfavorable seeds, selecting a favorable network, selecting a favorable dimension, or reopening real outcomes.

### 2.4 B3 graph-topology alternative

An outcome-free topology-spectrum audit tested whether the external regulon graph itself supplied an obvious compact low-dimensional basis:

- workflow run `34798559079`;
- artifact `10330477936`;
- artifact SHA-256 `ec28faedd4a1cfee32a288eebd5dd282bcd810ceab0c41e932d383bead27e30f`;
- expression opened `false`;
- TF activity scored `false`.

Both CollecTRI and DoRothEA ABC remain high-rank and distribute most spectral mass outside the first 2-5 modes. Early eigengaps do not license promotion of those first modes as a natural compact regulatory state.

This closes the immediate graph-mode shortcut. It does not prove that every future regulatory representation must fail.

### 2.5 G1 restoration

The exact unity route remains valid where the native model legitimately supplies the required restoration structure. The general directed heterogeneous-restoration case does not license a naive normalized unity rule, and the current SCC25 source work has not independently resolved a general restoration operator.

Therefore normalized empirical G1 remains a separate unresolved gate and is not used to rescue this cycle.

## 3. Function Map and Limit Map disposition

### Function Map retained

Within the frozen chronic weekly representation, the strongest repeatable functioning result is a shared low-dimensional D1 transition description with mathematical spectral-radius envelopes below the discrete-time unit circle, especially robust at `r=2`, while D2 treatment-specific operator reorganization is not supported.

This is a representation-specific dynamical architecture result. It is not relabeled as biological stability, health, resilience, damping, or `Chi_bio`.

### Limit Map retained

The current limits are equally important:

- short-term D1 predictive adequacy refused at both ranks;
- chronic `r=3` predictive adequacy is not robust to the stringent feature gate;
- non-normal/transient interpretation remains representation-sensitive;
- the preregistered B3 compressed state failed independent identifiability qualification;
- external regulon topology does not provide a compact natural 2-5D substitute;
- normalized empirical G1 remains blocked by restoration identifiability.

These limits are the reason the correct output is a bounded architecture map rather than a scalar `Chi_bio`.

## 4. Scalar, modal/vector, and conglomerate disposition

The three representation components remain complementary starting views, not a complete ontological decomposition and not ingredients to be averaged into a master score.

- **Scalar:** no system-level biological `Chi_bio` is admitted in this cycle.
- **Modal/vector:** the regulon topology audit indicates distributed high-rank organization rather than a naturally privileged compact 2-5D graph basis under the tested sources.
- **Conglomerate/system:** the previously established layered RNA, composition, genomic, protein/phosphoprotein, methylation/context, and temporal architecture remains the better-supported system description.

Disagreement and non-compressibility are preserved as scientific information.

## 5. v0.7.7 closure audit

The work in this cycle is classified as P0-D/P0-Q. No P1 or P2 promotion is claimed.

The closure preserves the current protocol requirements relevant here:

- failures and refusals remain visible;
- the B3 qualification failure is not counted as a successful scientific prediction;
- no post-result retuning is used to manufacture admission;
- standard predictive comparators used in the G2 lane remain visible;
- bounded foundational robustness is preserved rather than replacing the primary result;
- scalar, modal/vector, and conglomerate views remain separate;
- Atlas placement does not define the candidate;
- no lower-level mathematical unit-circle result is promoted into an unearned higher-level biological boundary;
- execution identity and supersession are checked before closure;
- the current closure record is durable and machine-readable.

MFR-14 is not claimed as satisfied for a new confirmatory `Chi_bio` prediction because this closure promotes no new confirmatory prediction.

Historical calculations executed under the then-current v0.7.4/v0.7.5 controls remain historical evidence. This closure audit uses v0.7.7 prospectively and does not retroactively promote or erase those results.

## 6. Re-entry conditions

The current cycle is closed, not permanently prohibited. Re-entry requires a genuinely new scientific basis rather than a favorable reformulation of the same failed path. At least one of the following must occur:

1. a materially distinct pre-outcome regulatory representation is specified under a new lineage, with explicit multiplicity debt and independent known-truth qualification;
2. independent restoration evidence resolves the G1 normalization problem strongly enough to reopen normalized empirical G1;
3. new untouched temporal or perturbational evidence supplies a system-level coordinate whose construction does not depend on Atlas placement, clinical outcome, or distance to unity.

Any such re-entry begins a new versioned P0-D/P0-Q cycle. The present failed B3 construction remains failed and visible.

## 7. Mechanical execution note

At the input head, the current `GRI v2 tests` pull-request run `34798656672` completed successfully. An obsolete run, `34749223154` at superseded commit `92c36323c4c4cb6c0a5bb12d92c5dd625739d811`, remains queued without a valid scientific reason. It is a mechanical orchestration orphan, is excluded from evidentiary state, and should be cancelled without rerun when an Actions-cancel capability is available.

The orphan does not block scientific closure because newer head-state CI completed successfully and the obsolete execution cannot contribute evidence.

## 8. Final claim ceiling

This cycle establishes neither an admitted biological Chi nor a biological unity boundary.

It does establish a sharper and more defensible result: the current GRI biological evidence supports a layered scalar + modal/vector + conglomerate architecture, with bounded temporal operator findings, while the tested routes to a single system-level `Chi_bio` fail or remain non-identifiable before admission.

**Final disposition:** `CLOSED_NO_ADMISSION`.
