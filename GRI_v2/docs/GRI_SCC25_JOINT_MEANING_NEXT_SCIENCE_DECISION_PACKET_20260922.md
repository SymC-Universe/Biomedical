# GRI SCC25 local/modal <-> capital-Chi next science decision packet - 22 September 2026

**Status:** SCIENTIFIC FREEZE REQUIRED BEFORE OUTCOME-BEARING EXECUTION  
**Program authority:** SymC General Operations Manual v0.8.3  
**Canonical branch:** `gri-conglomerate-v1-integration-20260921`  
**Scalar branch:** CLOSED / REFUSED FOR CURRENT SCC25 G2 REPRESENTATION  
**Biological outcome opening authorized:** NO

## 1. Why this packet exists

The SCC25 post-result scalar-admission diagnostic has now answered one previously open question:

> the current weekly G2 transition-operator representation does not license a biological lowercase scalar chi.

Rank 2 has no complex-conjugate pair across the required refits. Rank 3 has pair structure that is not invariant across the coequal frozen rank alternatives, and no continuous generator or logarithm branch is independently licensed.

Therefore the next joint-meaning investigation must use a **no-scalar local/modal branch** unless a future prospective derivation earns a scalar independently.

The remaining choices below are not mechanical. They determine the scientific question actually asked of SCC25 and must be frozen before the next outcome-bearing computation.

## 2. Fixed constraints already earned

The next design may not change these points:

1. `rho(T)` is not chi.
2. No logarithm branch is selected post hoc.
3. No continuous-time generator is assumed from one-week sampling.
4. Rank 2 and rank 3 remain coequal historical representations unless a new prospective criterion adjudicates them.
5. The short-term SCC25 G2 route failed its own frozen predictive-adequacy tests and remains a negative result.
6. Chronic G2 supports narrower representation-dependent transition-operator evidence only.
7. Proliferation is an external phenotype/readout and must not be folded into capital Chi merely to increase association.
8. The source paper's CoGAPS result is prior/source-native evidence for evolving molecular programs; it is not automatically a fair one-step forecast comparator.
9. Persistence and arm-specific mean-next-state are mandatory simple temporal baselines.
10. DMD/DMDc or a matched reduced-rank/state-space family is the established comparator family for transition prediction.
11. Any local/modal versus capital-Chi added-value claim requires held-out information not algebraically guaranteed by shared construction.
12. A failed or non-identifiable result remains valid and may not be rescued on the same outcome.

## 3. Decision A - local/modal object

The scalar route is refused. One local/modal representation still has to be prospectively declared.

### A1. Full discrete operator/eigenspectrum record
Use the fitted D1 operator plus rank, eigenvalues/eigenvectors, spectral radius, and refusal/identifiability flags as the local/modal object.

**Strength:** preserves the full dynamical object already supported by the source-native state-space route.

**Risk:** high-dimensional representation; added-value tests need careful dimensional matching.

### A2. Predeclared low-dimensional modal feature set
Use a small prospectively frozen subset such as spectral radius, real-mode contribution, pair-presence/refusal state, and other branch-free discrete-time invariants.

**Strength:** simpler held-out comparison.

**Risk:** feature selection itself is a scientific compression and must be justified without outcome tuning.

### A3. Both A1 and A2 as coequal representation-sensitivity tracks
Run the full object and one prospectively frozen compressed modal track as coequal analyses.

**Strength:** directly tests whether any conclusion depends on compression.

**Risk:** larger qualification family and multiplicity/search accounting.

### Decision required
Freeze A1, A2, or A3 and, for A2/A3, the exact features before any target outcome is evaluated.

## 4. Decision B - partial SCC25 capital-Chi blocks

The current design proposal is:

`Chi_SCC25(t) = {R(t), S(t), M(t), T(t), Q(t)}`

where:
- R = RNA/regulatory-state organization;
- S = methylation/epigenetic organization;
- M = modal/vector structure;
- T = directly ordered temporal/operator structure;
- Q = quality/uncertainty/refusal metadata.

### B1. Full proposed partial carrier
Retain R/S/M/T/Q as separate typed blocks.

### B2. Minimal R/S/M carrier plus Q metadata
Keep temporal operator information exclusively in the local/modal side so that T is not algebraically duplicated inside the broader carrier.

**Strength:** cleaner local-versus-system incremental-information test.

**Risk:** broader capital-Chi object is intentionally narrower than the full architecture.

### B3. Two nested carriers
Primary: R/S/M + Q.  
Sensitivity: R/S/M/T + Q.

**Strength:** directly measures how much any capital-Chi advantage is merely due to re-importing operator information.

**Risk:** adds one prespecified sensitivity family.

### Decision required
Freeze B1, B2, or B3. If B3 is chosen, the primary versus sensitivity status must be explicit.

## 5. Decision C - state representation and normalization

Choices must be declared separately for RNA and methylation. The scientific issue is whether system organization is robust to reasonable representation rather than an artifact of scaling.

At minimum freeze:
- source-level transform inherited from the qualified SCC25 route;
- centering/scaling rule;
- whether transforms are fit on training states only;
- missingness handling;
- feature gate;
- rank/state-reduction rule;
- whether a second representation is a formal sensitivity analysis.

No held-out state may influence fitted transforms.

## 6. Decision D - source-native CoGAPS role

### D1. Published-prior comparator
Use the published CoGAPS findings as source-native qualitative prior evidence only; do not reproduce CoGAPS in this execution.

### D2. Direct source-native reproduction
Reproduce the published CoGAPS time-course decomposition under a separately pinned environment before using it as the description comparator.

### D3. Reproduction as a parallel comparator lane
Run D2, but keep its descriptive task separate from the predictive DMD/state-space comparison.

### Decision required
Freeze D1, D2, or D3. CoGAPS may not be scored as a forecast comparator unless the target question is changed and frozen accordingly.

## 7. Decision E - held-out temporal task

The target unit must preserve the ordered experimental structure.

Candidate task families:

### E1. Leave-one-transition-out within arm
Predict the next state from the preceding state while leaving one transition out.

### E2. Blocked late-time forecast
Fit on earlier weeks, evaluate later transitions.

### E3. Leave-one-week-pair-out across both arms with arm retained as context
Tests transport across time while preserving treatment/control identity.

### E4. More than one task, explicitly primary plus sensitivity
A single primary task plus one prespecified sensitivity is allowed; they may not be treated as independent confirmation.

### Decision required
Freeze one primary task, the exact eligible transition set, and any sensitivity task before computation.

## 8. Decision F - incremental-value question

The GOM joint-meaning requirement can be asked in both directions, but the metric and nesting must be frozen.

Required nested comparisons:

1. simple temporal baseline;
2. local/modal object only;
3. capital-Chi object only;
4. local/modal + capital-Chi;
5. established native comparator under the same information budget.

Candidate primary metrics:
- held-out R2 for continuous state targets;
- held-out normalized MSE;
- log predictive density if a probabilistic model is prospectively specified.

At least one effect-size criterion must be frozen. A p-value alone is not sufficient.

## 9. Decision G - representation dependence and refusal

Before execution define what happens if:
- rank 2 and rank 3 disagree materially;
- the capital-Chi carrier changes conclusion under an admissible alternate state representation;
- one block is non-identifiable/missing;
- modal features do not transport;
- native/simple comparators equal or beat the proposed architecture.

Allowed outcomes must include:
- local/modal sufficient;
- broader Chi adds information;
- local/modal adds information conditional on Chi;
- complementary information;
- representation-dependent / not promotable;
- standard toolkit equivalent or better;
- neither adequate;
- no coherent scalar, multirepresentational architecture retained descriptively.

## 10. Decision H - stopping rule

Freeze a finite stopping rule before the first outcome is opened.

Minimum rule:
- no post-result feature addition;
- no post-result rank selection;
- no post-result block addition;
- no threshold tuning on the held-out transitions;
- one planned representation sensitivity does not create a new primary;
- if the primary task fails, it remains failed and any follow-up is a new P0-Q version.

## 11. Recommended lowest-complexity design for discussion, not frozen

The smallest design that directly answers the current GOM question without manufacturing a scalar is:

- A3: full operator plus one frozen compressed discrete-modal sensitivity;
- B3: primary R/S/M+Q carrier, T-added sensitivity;
- D3: CoGAPS reproduction kept in its native descriptive lane;
- E2: blocked late-time forecast as primary, one leave-one-transition sensitivity;
- nested simple -> modal -> capital-Chi -> combined -> native comparator evaluation;
- primary held-out nMSE/R2 plus fixed effect-size floor;
- explicit representation-dependence refusal;
- finite one-primary/one-sensitivity stop rule.

This is a proposed design because it minimizes algebraic duplication between local/modal and capital-Chi while directly testing whether system organization adds held-out information. It is **not** authorized until the user approves or modifies the scientific choices.

## 12. Parallel work that does not require this decision

The following may continue without opening the SCC25 joint outcome:
- exact source hashing and R/S materialization preparation;
- external GEO identity/source manifests;
- native-comparator installation/provenance checks;
- manuscript claim-delta mapping;
- editor-contact preparation;
- MFR-14 scaffolding;
- reproducibility and source-of-record synchronization.

## User decision required

Approve or modify Decisions A-H before the next SCC25 local/modal <-> capital-Chi outcome-bearing run.
