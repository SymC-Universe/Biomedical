# GRI C1 carrier materialization recovery-boundary audit - 22 September 2026

**Status:** COMPLETE RECOVERY / RUN-ECONOMY AUDIT  
**Program authority:** SymC General Operations Manual v0.8.3  
**Historical Stage C1 result:** IMMUTABLE / COMPLETE  
**New biological computation:** NONE

## Question

Can the current capital-Chi carrier work proceed from already-retained Stage C1 outputs, or is a multi-gigabyte Stage C1 recomputation actually necessary?

## Historical C1 state recovered

The retained final C1 package establishes:

- `STAGE_C1_COMPLETE`;
- 32 cancers;
- 9,457 shared eligible samples;
- 100 resamples per cancer;
- C1-1, C1-2 and C1-3 promotion criteria passed under the frozen historical contract;
- no biological chi used;
- no historical GRI scalar used;
- claim ceiling remains static preregistered methylation/RNA architecture only.

The retained output-hash manifest identifies the compact final outputs, including:
- C1 cancer-level effects;
- global inference;
- Hallmark coupling detail;
- mode-contribution strata;
- resample membership;
- resample summaries;
- sample/probe eligibility;
- RNA zero-information audit/draws;
- local-heavy-output manifest.

## Heavy-output boundary

The historical execution deliberately retained per-cancer full top-five probe-contribution arrays as local heavy outputs rather than embedding their multi-gigabyte bytes in the compact result ZIP.

The connected retained evidence contains:
- exact heavy-output filenames;
- byte sizes;
- SHA-256 identities;
- ordered probe-sidecar identities.

It does **not** currently expose the corresponding NPY bytes through the connected project/library surface.

This is a provenance-complete absence, not permission to recreate or approximate the arrays from summary statistics.

## What can proceed without rerunning Stage C1

### Cancer-level read-only export

`gri_Chi_bio_internal_export_schema_v0_2.json` was explicitly designed to package cancer-level modal, cross-layer, robustness and context summaries **without rerunning Stage C1** and without computing a Chi-bio value.

That route remains valid and should be preferred for:
- dependency/identifiability auditing;
- claim-evidence synchronization;
- cancer-level architecture summaries;
- planning of later C2/C3 tests that do not require patient-level raw carrier values.

### Source/provenance closure

Exact R/S source identity, code/config identities, eligibility rules, membership definitions, final-result hashes and recovery provenance can continue to be closed mechanically.

## What cannot be honestly reconstructed from the compact package alone

The current patient/system long-form C1 carrier contract requires entity-level block measurements.

For the S/M patient-level carrier, the compact connected package does not expose every heavy local array needed to reproduce the exact original full contribution representation byte-for-byte.

Therefore:

`PATIENT_LEVEL_FULL_C1_CARRIER_FROM_CONNECTED_COMPACT_ARCHIVE = NOT_CURRENTLY_MATERIALIZABLE_WITHOUT_ADDITIONAL_BYTES_OR_RECOMPUTATION`.

This does not invalidate the historical C1 result.

## Recompute rule

Do **not** rerun the 5 GB Stage C1 analysis merely because the current capital-Chi architecture can conceptually consume patient-level values.

A recomputation becomes justified only if:
1. the prospectively frozen next scientific task actually requires patient-level S/M carrier values;
2. the exact local-heavy bytes cannot be recovered from the original machine/archive;
3. no lower-cost, historically frozen representation answers the same task;
4. the rerun uses the existing frozen C1 definitions and is explicitly labeled reconstruction, not untouched evidence.

If original local-heavy bytes become available, verify them against the recorded SHA-256 values and use those bytes instead of recomputing.

## Current run-economy disposition

- historical Stage C1: **do not rerun now**;
- cancer-level internal export: **safe to reconstruct from retained final-result artifacts when bytes are available**;
- full patient-level C1 carrier: **defer until the next frozen task proves it is necessary**;
- R/S exact source hashing: **continue**;
- external source identity/crosswalk work: **continue**;
- SCC25 joint-meaning outcome run: **blocked on prospective science freeze**.

## Scientific consequence

The inability to materialize one heavy representation immediately is not evidence for or against capital Chi. It is an execution/provenance boundary.

No new summary feature, imputation, compressed surrogate, or master score may be invented solely to avoid the missing heavy bytes.
