# Stage D User Instruction Source

When ChatGPT gives user-facing execution instructions, this repository remains authoritative for the scientific order.

Current exact user instructions:
- Prefer GitHub metadata preflight automatically.
- If that fails only because NCBI metadata access is unavailable, use Kaggle Phase A exactly as documented.
- Do not begin numeric biological outcome analysis until D0.3 is frozen.
- Preserve all runner/checkpoint state.
- Return generated ZIP/log artifacts without manually editing them.
