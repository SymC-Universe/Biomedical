# Stage D Integrity Note

The Stage-D branch intentionally contains many small explicit freeze documents rather than one mutable omnibus file. This makes later amendments auditable: a changed scientific rule must identify the specific frozen object it supersedes and when the change occurred relative to biological outcome inspection.
